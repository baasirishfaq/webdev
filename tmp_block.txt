      gap: 0.75rem;
      animation: slideIn 0.5s ease-out;
    }

    .cmd::before {
      content: "▶";
      color: var(--accent-purple);
      font-size: 1.2em;
      animation: pulse 1.5s ease-in-out infinite;
    }

    @keyframes pulse {

      0%,
